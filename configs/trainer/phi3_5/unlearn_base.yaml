defaults:
  - /trainer/finetune

# Conservative defaults for Phi-3.5 to prevent catastrophic forgetting
args:
  learning_rate: 5e-6
  num_train_epochs: 5
  lr_scheduler_type: cosine
  warmup_ratio: 0.1
